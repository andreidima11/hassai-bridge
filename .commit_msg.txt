v0.1.9.4-beta: Major UI overhaul — modals to inline pages, code editor rebuild

UI Architecture:
- Replaced all modal overlays with inline page views (no more popups)
  - Users: add user page, user detail page
  - Conversations: conversation detail page
  - Memories: add memory page
  - Skills: skill editor page
  - Providers: provider edit page, secondary provider edit page
- Unified back-navigation with .page-back component
- Escape key handles all inline pages

Skill Code Editor:
- Complete rebuild of syntax-highlighted code editor
- Fixed ghost scrollbar bug (textarea overflow:hidden + auto-resize)
- Container handles all scrolling, line numbers stay synced
- Max-height constraint prevents page stretching

Provider Management:
- Test button moved inside each provider's edit page
- Inline test results with status display
- Removed test buttons from provider list cards

Memory List:
- Removed per-item delete buttons (use bulk select instead)
- Pagination and action buttons layout improved

Settings:
- Mobile-friendly settings subtabs (hidden scrollbar, larger tap targets)
- 3-column grids collapse to 1-column on mobile

Cache Buster:
- Fixed: now uses VERSION.timestamp instead of just VERSION
- Every server restart forces fresh asset loading

i18n:
- 70+ Romanian translations fixed: Title Case to sentence case
- Added settings.testConnection key (EN/RO)
- Fixed Lista Memoriilor capitalization

Visual Polish:
- Back button uses --text color with opacity for better readability
- Stats detail rows: no-wrap + text-overflow ellipsis
- Memory list title separator
- Delete All button removed from memory list
- Button spacing improvements
